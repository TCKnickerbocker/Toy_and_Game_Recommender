import { Box, Typography } from "@mui/material";
import { RateItem } from "../../components/RateItem";

export default function NewUserPage() {
  return (
    <Typography>
      Hey there! Welcome to our Toy and Game Recommender. Since it's your first
      time here, lets have you rate some items to get a good idea of what you're
      looking for!
    </Typography>
  );
}
